# 灵智智能体系统 — 架构收敛与治理开发任务清单

> 状态标记: ⬜ 未开始 | 🔲 进行中 | ✅ 已完成 | ❌ 已取消

---

## P0 核心收敛（必须优先完成，阻塞后续所有架构演进）

---

### P0-1 统一执行提交内核（Execution Submission Kernel）

**问题**: `orchestrator/routes.execute.ts`、`orchestrator/routes.closedLoop.ts`、`agent-runtime/routes.ts`、`collab-runtime/routes.ts` 各自重复实现工具解析→权限校验→能力包络→审批判定→step 构造→run/job 状态推进→入队→审计摘要的完整逻辑。

| # | 任务 | 交付物 | 稳定性验证 | 状态 |
|---|------|--------|-----------|------|
| 1.1 | 从四个入口提取共用执行逻辑，创建 `apps/api/src/kernel/executionKernel.ts` | 统一模块：`submitExecution(params) → ExecutionReceipt` | 单元测试覆盖全部分支（allow/deny/approval/error）| ⬜ |
| 1.2 | 统一工具解析流程：toolRef 解析 → resolveEffectiveToolRef → getToolVersionByRef → isToolEnabled | 函数 `resolveAndValidateTool()` | 对比测试：新旧路径产出一致 | ⬜ |
| 1.3 | 统一权限校验 + 能力包络构建：authorize → admitToolExecution → capabilityEnvelope 组装 | 函数 `buildExecutionEnvelope()` | contract test：envelope 字段完整性断言 | ⬜ |
| 1.4 | 统一审批判定逻辑：approvalRequired / riskLevel / policy 综合判定 | 函数 `determineApproval()` | 覆盖 low/medium/high risk × approval on/off 矩阵 | ⬜ |
| 1.5 | 统一 step 构造与 run/job 状态推进：createJobRun → appendStepToRun → setRunAndJobStatus → enqueueWorkflowStep | 函数 `createAndEnqueueStep()` | 数据库事务一致性测试 | ⬜ |
| 1.6 | 统一错误分类与 receipt 结构 | 类型 `ExecutionReceipt`（含 errorCode/errorCategory/traceId/auditDigest） | 类型覆盖率检查 + 所有入口返回格式一致 | ⬜ |
| 1.7 | 改造 `orchestrator/routes.execute.ts`，调用统一内核 | 改造后的 routes.execute.ts | 回归测试：原有 API 行为不变 | ⬜ |
| 1.8 | 改造 `orchestrator/routes.closedLoop.ts` 执行部分，调用统一内核 | 改造后的 routes.closedLoop.ts | 回归测试 | ⬜ |
| 1.9 | 改造 `agent-runtime/routes.ts` 执行部分，调用统一内核 | 改造后的 routes.ts | 回归测试 | ⬜ |
| 1.10 | 改造 `collab-runtime/routes.ts` 执行部分，调用统一内核 | 改造后的 routes.ts | 回归测试 | ⬜ |
| 1.11 | 删除各入口中已提取到内核的重复代码 | 代码行数减少 ≥30% | 全量 API 冒烟测试 | ⬜ |

**涉及文件**:
- 新建: `apps/api/src/kernel/executionKernel.ts`
- 改造: `apps/api/src/skills/orchestrator/routes.execute.ts` (282行)
- 改造: `apps/api/src/skills/orchestrator/routes.closedLoop.ts` (801行)
- 改造: `apps/api/src/skills/agent-runtime/routes.ts` (460行)
- 改造: `apps/api/src/skills/collab-runtime/routes.ts` (1228行)
- 依赖: `apps/api/src/modules/tools/executionAdmission.ts`
- 依赖: `apps/api/src/modules/workflow/jobRepo.ts`
- 依赖: `apps/api/src/modules/workflow/queue.ts`

---

### P0-2 统一规划内核（Planning Kernel）

**问题**: `orchestrator/modules/orchestrator.ts`、`agent-runtime/routes.ts`、`collab-runtime/routes.ts` 各自复制"工具发现 → tool catalog 组装 → LLM 规划提示 → tool_call 解析 → 建议过滤 → 失败分类"的完整规划流程。

| # | 任务 | 交付物 | 稳定性验证 | 状态 |
|---|------|--------|-----------|------|
| 2.1 | 创建 `apps/api/src/kernel/planningKernel.ts`，抽取统一规划内核 | 函数 `planExecution(params) → PlanResult` | 单元测试 | ⬜ |
| 2.2 | 统一工具发现与 catalog 组装：从 `discoverEnabledTools` 收敛 | 函数 `buildToolCatalog()` | 对比测试：三个入口发现结果一致 | ⬜ |
| 2.3 | 统一 LLM 规划提示模板与调用 | 函数 `invokePlannerLLM()` + 可配置提示模板 | 提示模板版本化测试 | ⬜ |
| 2.4 | 统一 tool_call 解析逻辑（当前 `parseToolCallsFromOutput` 在多处调用） | 函数 `parseAndValidateToolCalls()` | 解析正确性 + 边界值测试 | ⬜ |
| 2.5 | 统一建议过滤：isToolEnabled + resolveEffectiveToolRef + 权限 + 写入限制 | 函数 `filterSuggestions()` | 覆盖 enabled/disabled/unresolved/denied 分支 | ⬜ |
| 2.6 | 统一失败分类语义与指标口径 | 枚举 `PlanFailureCategory`: model_error / parse_error / no_tools / no_enabled_suggestion / empty | 指标埋点一致性测试 | ⬜ |
| 2.7 | 改造 `orchestrator/modules/orchestrator.ts` 使用统一规划内核 | 改造后文件 | 回归测试 | ⬜ |
| 2.8 | 改造 `agent-runtime/routes.ts` 规划部分 | 改造后文件 | 回归测试 | ⬜ |
| 2.9 | 改造 `collab-runtime/routes.ts` 规划部分 | 改造后文件 | 回归测试 | ⬜ |
| 2.10 | 为规划内核补齐 metrics 埋点：`app.metrics.incAgentPlanFailed` 统一入口 | 统一指标 key | 指标产出验证 | ⬜ |

**涉及文件**:
- 新建: `apps/api/src/kernel/planningKernel.ts`
- 改造: `apps/api/src/skills/orchestrator/modules/orchestrator.ts` (16.5KB)
- 改造: `apps/api/src/skills/orchestrator/modules/planner.ts` (140行)
- 改造: `apps/api/src/skills/agent-runtime/routes.ts`
- 改造: `apps/api/src/skills/collab-runtime/routes.ts`
- 依赖: `apps/api/src/lib/llm.ts`

---

### P0-3 明确运行时职责边界

**问题**: `orchestrator.turn`（单轮对话）、`orchestrator.execute`（单步执行）、`orchestrator.closed-loop`（闭环执行）、`agent-runtime`（自主代理）、`collab-runtime`（多智能体协作）职责交叉，新功能倾向跨入口复制实现。

| # | 任务 | 交付物 | 稳定性验证 | 状态 |
|---|------|--------|-----------|------|
| 3.1 | 编写运行时职责矩阵文档：定义四类标准入口及其唯一职责 | 职责矩阵（turn=对话/execute=单步/closed-loop=闭环/collab=协作） | 团队评审确认 | ⬜ |
| 3.2 | 审查 `routes.turn.ts` + `routes.turnStream.ts`，确保仅承担对话职责 | 代码审查报告 + 必要重构 | 对话路径端到端测试 | ⬜ |
| 3.3 | 审查 `routes.execute.ts`，确保仅承担单步执行职责 | 代码审查报告 + 必要重构 | 单步执行端到端测试 | ⬜ |
| 3.4 | 审查 `routes.closedLoop.ts`（801行），拆分规划/执行/审查/重试子模块 | 拆分后 ≤ 3 个文件 | 闭环流程端到端测试 | ⬜ |
| 3.5 | 审查 `agent-runtime/routes.ts`（460行），确保与 closed-loop 无职责重叠 | 代码审查报告 + 去重改造 | 自主代理端到端测试 | ⬜ |
| 3.6 | 审查 `collab-runtime/routes.ts`（1228行），拆分为规划/执行/仲裁/查询子模块 | 拆分后 ≤ 4 个文件 | 协作流程端到端测试 | ⬜ |
| 3.7 | 建立入口准入规则：新增功能必须声明所属入口，禁止跨入口复制 | lint 规则或 CONTRIBUTING 约定 | CI 检查 | ⬜ |

**涉及文件**:
- 审查/拆分: `apps/api/src/skills/orchestrator/routes.turn.ts` (3.9KB)
- 审查/拆分: `apps/api/src/skills/orchestrator/routes.turnStream.ts` (19.9KB)
- 审查/拆分: `apps/api/src/skills/orchestrator/routes.execute.ts` (13.9KB)
- 审查/拆分: `apps/api/src/skills/orchestrator/routes.closedLoop.ts` (51.9KB)
- 审查/拆分: `apps/api/src/skills/agent-runtime/routes.ts` (22.2KB)
- 审查/拆分: `apps/api/src/skills/collab-runtime/routes.ts` (61.9KB)

---

### P0-4 收敛 Skill 概念分层

**问题**: `entity.kernel`（平台核心工具声明）、`nl2ui.generator/memory-manager/...`（内建能力插件）、`skills/` 目录下 manifest.json（动态沙箱 Skill）三者在注册表、代码、文档中概念混用。

| # | 任务 | 交付物 | 稳定性验证 | 状态 |
|---|------|--------|-----------|------|
| 4.1 | 定义三层分层命名规范：kernel-tool / builtin-capability / sandbox-extension | 命名规范文档 | 团队评审 | ⬜ |
| 4.2 | 修改 `registry.ts`：按分层注释与分组注册 | 改造后 registry.ts | 启动正常 + 注册数量一致 | ⬜ |
| 4.3 | 修改 `skillPlugin.ts`：`BuiltinSkillPlugin` 增加 `layer` 字段（kernel/builtin/extension） | 改造后 skillPlugin.ts | 类型检查通过 | ⬜ |
| 4.4 | 修改 `toolAutoDiscovery.ts`：对不同层级工具标记 source layer | 改造后 toolAutoDiscovery.ts | 注册结果含 layer 标记 | ⬜ |
| 4.5 | 修改 `tool_definitions` 表结构：增加 `source_layer` 列 | migration 文件 | 迁移可回滚 | ⬜ |
| 4.6 | 修改治理接口：工具列表/详情 API 返回 layer 信息 | API 返回值变更 | API 兼容性测试 | ⬜ |

**涉及文件**:
- 改造: `apps/api/src/skills/registry.ts` (156行)
- 改造: `apps/api/src/lib/skillPlugin.ts`
- 改造: `apps/api/src/modules/tools/toolAutoDiscovery.ts` (282行)
- 新建: migration 文件
- 改造: `apps/api/src/modules/governance/toolGovernanceRepo.ts`

---

### P0-5 修正自动发现与默认启用策略

**问题**: `toolAutoDiscovery.ts` 第 251-271 行"发现即对所有 tenant + 所有 space 默认 enable"，违反"默认拒绝"不变式。

| # | 任务 | 交付物 | 稳定性验证 | 状态 |
|---|------|--------|-----------|------|
| 5.1 | 修改 `autoDiscoverAndRegisterTools`：只注册 definition/version，不自动写 rollout | 改造后 toolAutoDiscovery.ts | 注册后 tool_rollouts 无新增行 | ⬜ |
| 5.2 | 将 tool 启用动作收敛到 governance 发布流程 | `toolGovernanceRepo.enableToolForScope()` | 启用/禁用流程端到端测试 | ⬜ |
| 5.3 | 为 kernel-tool 层级工具保留自动启用（仅 entity.create/update/delete 等核心） | 白名单机制 | kernel 工具启动后可用 | ⬜ |
| 5.4 | 为未显式启用工具增加默认拒绝逻辑 | `isToolEnabled` 默认返回 false | 未启用工具调用被拒测试 | ⬜ |
| 5.5 | 补充审计事件：工具启用/禁用变更写入 audit | audit 事件记录 | 审计日志完整性验证 | ⬜ |

**涉及文件**:
- 改造: `apps/api/src/modules/tools/toolAutoDiscovery.ts` (重点 192-277行)
- 改造: `apps/api/src/modules/governance/toolGovernanceRepo.ts`
- 依赖: `apps/api/src/modules/audit/auditRepo.ts`

---

### P0-6 修复启动时序与注册时序

**问题**: `registry.ts` console.log 提示 `built-in skill registry not available yet, skipping`，说明 toolAutoDiscovery 可能在 registry 初始化之前执行。

| # | 任务 | 交付物 | 稳定性验证 | 状态 |
|---|------|--------|-----------|------|
| 6.1 | 梳理 server.ts 启动时序：initBuiltinSkills → route register → toolAutoDiscovery | 时序图文档 | 启动日志无 warning | ⬜ |
| 6.2 | 确保 `initBuiltinSkills()` 在 `autoDiscoverAndRegisterTools()` 之前完成 | 改造后 server.ts | 启动顺序断言测试 | ⬜ |
| 6.3 | 消除 `collectBuiltinSkillTools` 中 try-catch 静默吞错 | 改为启动阶段抛错 + 日志降级 | 错误可观测 | ⬜ |
| 6.4 | 添加启动阶段一致性检查：注册的 tool_definitions 与 routes 数量匹配 | 启动后自检函数 | CI 启动自检 | ⬜ |
| 6.5 | 补启动阶段回归测试 | 测试文件 | CI 集成 | ⬜ |

**涉及文件**:
- 改造: `apps/api/src/server.ts`（启动入口）
- 改造: `apps/api/src/skills/registry.ts`
- 改造: `apps/api/src/modules/tools/toolAutoDiscovery.ts`

---

## P1 架构完善（P0 完成后推进，提升规划质量与运行时一致性）

---

### P1-7 升级 planner，从启发式过渡到可治理规划器

**问题**: `planner.ts` 的 `scoreCandidate` 仅靠字符串匹配和硬编码罚分，无法利用 tool metadata 的丰富信息。

| # | 任务 | 交付物 | 稳定性验证 | 状态 |
|---|------|--------|-----------|------|
| 7.1 | 将 `scoreCandidate` 升级为结构化评分引擎 | 新 `structuredScore()` 支持权重配置 | 评分可解释性测试 | ⬜ |
| 7.2 | 引入 tool metadata 多维权重：scope / risk / approval / cost / resourceType / action | 权重配置结构 `ScoringWeights` | 权重调整不影响排序稳定性 | ⬜ |
| 7.3 | 引入历史成功率权重（从 audit/metrics 获取） | 历史成功率查询函数 | 冷启动降级（无数据时忽略） | ⬜ |
| 7.4 | 为 planner 增加 explain 输出：选择/拒绝原因 | `PlanExplanation` 类型 | explain 字段非空验证 | ⬜ |
| 7.5 | 建立 plan_revision / replan / fallback 统一机制 | `replanOnFailure()` 函数 | replan 次数上限 + 死循环防护 | ⬜ |

**涉及文件**:
- 改造: `apps/api/src/skills/orchestrator/modules/planner.ts` (140行)
- 依赖: `apps/api/src/modules/governance/observabilityRepo.ts`

---

### P1-8 建立统一运行时状态机

**问题**: `runs/steps/taskState/collabState` 的状态转换分散在 `processStep.ts`(51.9KB)、`collab-runtime/routes.ts`、`collab-runtime/modules/reducer.ts`，各自推进状态。

| # | 任务 | 交付物 | 稳定性验证 | 状态 |
|---|------|--------|-----------|------|
| 8.1 | 定义标准状态机：Run 状态 + Step 状态 + 合法转换表 | `apps/api/src/kernel/stateMachine.ts` | 状态转换穷举测试 | ⬜ |
| 8.2 | 收敛 Step 状态：pending / running / needs_approval / needs_device / needs_arbiter / succeeded / failed / deadletter / canceled | 统一枚举 `StepStatus` | 类型安全 + 编译通过 | ⬜ |
| 8.3 | 收敛 Run 状态：created / queued / running / needs_approval / succeeded / failed / stopped | 统一枚举 `RunStatus` | 类型安全 | ⬜ |
| 8.4 | 收敛 CollabRun 状态：对齐 `reducer.ts` 的 `CollabDerivedState.phase` | 统一枚举 `CollabPhase` | 与 reducer 计算一致 | ⬜ |
| 8.5 | 将 `processStep.ts` 状态推进逻辑替换为状态机调用 | 改造后 processStep.ts | 回归测试 | ⬜ |
| 8.6 | 将 `collab-runtime` 状态推进逻辑替换为状态机调用 | 改造后 collab-runtime | 回归测试 | ⬜ |
| 8.7 | 建立 invariant 检查：状态不一致时告警 | `checkStateInvariant()` 函数 | 故意注入不合法转换验证告警 | ⬜ |

**涉及文件**:
- 新建: `apps/api/src/kernel/stateMachine.ts`
- 改造: `apps/worker/src/workflow/processor/processStep.ts` (51.9KB)
- 改造: `apps/api/src/skills/collab-runtime/modules/reducer.ts` (5.6KB)
- 改造: `apps/worker/src/index.ts` (32KB)

---

### P1-9 收敛配置来源，减少环境变量漂移

**问题**: `SKILL_RUNTIME_UNSAFE_ALLOW`、`SKILL_ISOLATION_MIN`、`SKILL_PACKAGE_ROOTS` 等大量 env 分散在各处，运行时治理配置与 bootstrap 配置混合。

| # | 任务 | 交付物 | 稳定性验证 | 状态 |
|---|------|--------|-----------|------|
| 9.1 | 梳理全部 env 变量清单（API / Worker / Runner / Device-Agent） | 环境变量清单（名称/来源/用途/是否运行时可变） | 覆盖率 100% | ⬜ |
| 9.2 | 划分 bootstrap 级（启动时固定）vs runtime 级（运行时可变） | 分类标记 | 团队评审 | ⬜ |
| 9.3 | 将 runtime 级配置下沉到 governance control plane（数据库/Redis） | 配置读取函数迁移 | 配置变更生效测试 | ⬜ |
| 9.4 | 建立配置优先级：env → governance → default | 优先级解析函数 | 优先级覆盖测试 | ⬜ |
| 9.5 | 建立配置生效摘要与审计记录 | 配置变更审计事件 | 审计日志验证 | ⬜ |

**涉及文件**:
- 扫描: `apps/worker/src/workflow/processor/dynamicSkill.ts`（env 使用密集）
- 扫描: `apps/worker/src/workflow/processor/dynamicSkillConfig.ts`
- 扫描: `apps/worker/src/config.ts`
- 改造: `apps/api/src/modules/governance/` 相关文件

---

### P1-10 精简默认内建能力集

**问题**: `registry.ts` 注册了 29 个 builtin skill（Phase 0-10），其中 `analyticsEngine`、`aiEventReasoning`、`mediaPipeline`、`replayViewer` 等属于上层扩展，不应默认加载。

| # | 任务 | 交付物 | 稳定性验证 | 状态 |
|---|------|--------|-----------|------|
| 10.1 | 划分"OS 核心能力"与"可选扩展能力"清单 | 分类清单 | 团队评审 | ⬜ |
| 10.2 | OS 核心（建议保留）：entityKernel / orchestrator / modelGateway / knowledgeRag / memoryManager / safetyPolicy / connectorManager / taskManager / channelGateway / triggerEngine | 核心清单 | 最小闭环可运行 | ⬜ |
| 10.3 | 可选扩展（建议降级）：analyticsEngine / aiEventReasoning / mediaPipeline / replayViewer / backupManager / artifactManager / identityLink / userViewPrefs / yjsCollab | 可选清单 | 禁用后核心不受影响 | ⬜ |
| 10.4 | 修改 `initBuiltinSkills()`：增加 `enabledSkills` 配置控制，默认仅注册核心集 | 改造后 registry.ts | 启动参数化测试 | ⬜ |
| 10.5 | 验证核心最小闭环稳定：仅注册核心集后全量冒烟测试 | 测试报告 | CI 通过 | ⬜ |

**涉及文件**:
- 改造: `apps/api/src/skills/registry.ts`
- 可能改造: `apps/api/src/server.ts`

---

### P1-11 统一供应链治理与运行时隔离判定

**问题**: API 的 `executionAdmission.ts`、Worker 的 `dynamicSkill.ts` / `dynamicSkillConfig.ts` / `supplyGate.ts`、Runner 的执行逻辑对信任/SBOM/scan/隔离等级判定标准不一致。

| # | 任务 | 交付物 | 稳定性验证 | 状态 |
|---|------|--------|-----------|------|
| 11.1 | 统一信任门槛判定：创建 `packages/shared/src/supplyChainPolicy.ts` | 共享模块 | API/Worker/Runner 引用同一函数 | ⬜ |
| 11.2 | 统一 process / container / remote 三类运行时最低要求 | 配置结构 `RuntimeIsolationPolicy` | 配置穷举测试 | ⬜ |
| 11.3 | 统一降级规则：container 不可用时是否允许降到 process | 降级函数 + 审计记录 | 降级审计事件验证 | ⬜ |
| 11.4 | 将降级行为纳入 metrics | 指标 `skill.runtime.degraded` | 指标产出验证 | ⬜ |

**涉及文件**:
- 新建: `packages/shared/src/supplyChainPolicy.ts`
- 改造: `apps/api/src/modules/tools/supplyGate.ts` (2.7KB)
- 改造: `apps/worker/src/workflow/processor/dynamicSkill.ts` (11KB)
- 改造: `apps/worker/src/workflow/processor/dynamicSkillConfig.ts` (3.2KB)

---

### P1-12 建立架构不变式检查

**问题**: "所有执行必须有 capabilityEnvelope / policySnapshot / traceId / audit 摘要"缺乏自动检查，旁路执行无法检测。

| # | 任务 | 交付物 | 稳定性验证 | 状态 |
|---|------|--------|-----------|------|
| 12.1 | 建立不变式检查函数：`assertExecutionInvariants(step)` | 检查函数 | 缺失字段时抛错 | ⬜ |
| 12.2 | 在 `processStep.ts` 入口处调用不变式检查 | 改造后 processStep.ts | 故意缺失字段验证报错 | ⬜ |
| 12.3 | 建立"旁路执行"检测测试：模拟绕过 API 直接提交 step | 测试文件 | CI 集成 | ⬜ |
| 12.4 | 建立"未注册/未启用工具执行"检测测试 | 测试文件 | CI 集成 | ⬜ |
| 12.5 | 建立重复入口执行语义一致性 contract test | 测试文件：同一 toolRef 从不同入口提交结果一致 | CI 集成 | ⬜ |
| 12.6 | 将关键不变式纳入 CI pipeline | CI 配置更新 | CI 红绿验证 | ⬜ |

**涉及文件**:
- 新建: `apps/api/src/kernel/invariants.ts`
- 新建: `apps/api/src/__tests__/invariants.test.ts`
- 改造: `apps/worker/src/workflow/processor/processStep.ts`

---

## P2 长期优化（P1 基本完成后推进，提升可维护性与可观测性）

---

### P2-13 建立能力分层目录重构方案

| # | 任务 | 交付物 | 稳定性验证 | 状态 |
|---|------|--------|-----------|------|
| 13.1 | 设计目标目录结构：kernel/ / planes/ / extensions/ / adapters/ | 目录设计文档 | 团队评审 | ⬜ |
| 13.2 | 将 P0-1/P0-2 产出的内核模块迁移到 kernel/ | 目录迁移 | import 全部正确 | ⬜ |
| 13.3 | 降低 skills/ 目录承载压力：核心平面模块移出 skills/ | 目录迁移 | 启动正常 | ⬜ |
| 13.4 | 强化模块边界：kernel 不允许 import skills/，skills/ 只能 import kernel/ 和 modules/ | lint 规则 | CI 检查 | ⬜ |

---

### P2-14 建立架构决策记录与废弃计划

| # | 任务 | 交付物 | 稳定性验证 | 状态 |
|---|------|--------|-----------|------|
| 14.1 | 为保留的入口建立 ADR（Architecture Decision Record） | ADR 文件 | 团队评审 | ⬜ |
| 14.2 | 为废弃入口/兼容层标记 `@deprecated` + 计划移除时间窗 | 代码标记 | 编译警告可见 | ⬜ |
| 14.3 | 清理历史兼容残留与 no-op 能力 | 清理 diff | 全量冒烟测试 | ⬜ |

---

### P2-15 建立 Agent OS 运营视图

| # | 任务 | 交付物 | 稳定性验证 | 状态 |
|---|------|--------|-----------|------|
| 15.1 | 输出核心指标：规划成功率 / 建议命中率 / 审批介入率 / 重试率 / replan率 / 默认拒绝率 | 指标定义 + 查询 SQL | 指标非空验证 | ⬜ |
| 15.2 | 按入口维度拆分指标：manual / orchestrator / agent-runtime / collab-runtime | 指标标签 | 维度正确性验证 | ⬜ |
| 15.3 | 将指标接入 `observabilityRepo.ts` 并暴露 API | API 端点 | API 返回结构验证 | ⬜ |
| 15.4 | 将架构质量问题转化为可观测告警（如旁路执行率 > 0、未注册工具调用率 > 0） | 告警规则 | 告警触发验证 | ⬜ |

**涉及文件**:
- 改造: `apps/api/src/modules/governance/observabilityRepo.ts` (8.2KB)
- 改造: `apps/api/src/modules/metrics/`

---

## 执行依赖关系

```
P0-6(启动时序) ──┐
                 ├──→ P0-4(Skill分层) ──→ P0-5(默认启用) 
P0-1(执行内核) ──┤                                      ├──→ P1-7 ~ P1-12
P0-2(规划内核) ──┤                                      │
P0-3(职责边界) ──┘                                      └──→ P2-13 ~ P2-15
```

**建议执行顺序**:
1. **第一批（可并行）**: P0-6 + P0-4
2. **第二批（可并行）**: P0-1 + P0-2 + P0-3
3. **第三批**: P0-5（依赖 P0-4 分层结果）
4. **第四批（可并行）**: P1-7 + P1-8 + P1-9
5. **第五批（可并行）**: P1-10 + P1-11 + P1-12
6. **第六批**: P2-13 → P2-14 → P2-15

---

## 全局稳定性保障措施

| 措施 | 说明 |
|------|------|
| 每个 P0 任务完成后全量冒烟测试 | 确保 API 启动 + 核心链路（对话→规划→执行→审计）可用 |
| 每个文件改造前后对比测试 | 新旧路径产出一致性断言 |
| 数据库迁移可回滚 | 每个 migration 必须有 down 脚本 |
| 关键不变式 CI 化 | capabilityEnvelope / policySnapshot / traceId / audit 不可缺失 |
| 单元测试覆盖率 ≥ 80% | 新增 kernel 模块必须达标 |
| 渐进式改造 | 先提取内核 → 再逐入口切换 → 最后删除旧代码，每步可回退 |

---

## 任务统计

| 优先级 | 任务组数 | 子任务数 | 预计工作量 |
|--------|---------|---------|-----------|
| P0 | 6 | 44 | 重度，约 60% 总工作量 |
| P1 | 6 | 35 | 中度，约 30% 总工作量 |
| P2 | 3 | 11 | 轻度，约 10% 总工作量 |
| **合计** | **15** | **90** | — |
