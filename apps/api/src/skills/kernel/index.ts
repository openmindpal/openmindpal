/**
 * Kernel Layer — Core platform tool declarations, always auto-enabled.
 *
 * The entity.kernel provides fundamental entity CRUD operations.
 * These tools are declared inline in registry.ts as they have no HTTP routes.
 */

// Entity kernel tools are defined inline in registry.ts
// This file serves as a logical grouping marker for the kernel layer.
export {};
